import heroImage from "../assets/img/launch-home.png";

const HeroImage = () => {
  return (
    <img src={heroImage} className="hero-image" alt="SpaceX rocket launch"/>
  )
}

export default HeroImage